// import type { FieldMetadata } from "@conform-to/react";

// interface FieldErrorProps {
//   field: FieldMetadata;
// }

// export default function FieldError({ field }) {
//   return (
//     <div
//       id={field?.errorId}
//       className="-mt-1 text-xs font-medium text-foreground-destructive"
//     >
//       {field.errors ? (
//         field.errors.map((error) => <p key={error}>{error}</p>)
//       ) : (
//         <p>&nbsp;</p> // Empty space to maintain layout
//       )}
//     </div>
//   );
// }
